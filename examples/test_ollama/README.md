# test_ollama

Minimal example used to verify that a local Python task can be created and executed with AI assistance.

Typical flow:

```bash
cd examples/test_ollama
python3 hello.py
```

You can also use it as a tiny sandbox after installing CASCADE:

```bash
cd examples/test_ollama
heal "replace hello world with a tiny CLI greeting"
```
