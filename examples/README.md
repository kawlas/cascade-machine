# Examples

This folder contains small example projects that show how CASCADE Machine can be used after installation.

Examples are not part of the installed runtime in `~/.cascade`.

Use them as learning material or smoke-test sandboxes.
